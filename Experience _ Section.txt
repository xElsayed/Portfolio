<section id="experience" class="py-20 section-hidden">
    <h2 class="text-3xl md:text-4xl font-mono font-bold text-white mb-16">
        <span class="text-cyber-primary">[</span> {{ module.section_title }} <span class="text-cyber-primary">]</span>
    </h2>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-16">
        <!-- Experience Timeline -->
        <div>
            <h3 class="font-mono text-2xl font-bold text-cyber-secondary mb-8">// Experience_Log</h3>
            <ol class="relative border-l border-cyber-muted/50">                  
                {% for job in module.experience_list %}
                <li class="mb-10 ml-6">
                    <span class="absolute flex items-center justify-center w-6 h-6 bg-cyber-primary rounded-full -left-3 ring-8 ring-cyber-bg">
                        <svg class="w-3 h-3 text-cyber-bg" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M6 2a1 1 0 00-1 1v1H4a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V6a2 2 0 00-2-2h-1V3a1 1 0 10-2 0v1H7V3a1 1 0 00-1-1zm0 5a1 1 0 000 2h8a1 1 0 100-2H6z" clip-rule="evenodd"></path></svg>
                    </span>
                    <h4 class="flex items-center mb-1 text-lg font-semibold text-white">{{ job.role }}</h4>
                    <time class="block mb-2 text-sm font-normal leading-none text-cyber-muted">{{ job.date }}</time>
                    <div class="text-base font-normal text-cyber-text">{{ job.description }}</div>
                </li>
                {% endfor %}
            </ol>
        </div>
        
        <!-- Certifications -->
        <div>
            <h3 class="font-mono text-2xl font-bold text-cyber-secondary mb-8">// Certification_Badges</h3>
            <div class="flex flex-wrap gap-4">
                {% for cert in module.cert_list %}
                <div class="bg-cyber-surface border border-cyber-primary/50 text-center p-4 rounded-lg flex-grow">
                    <span class="font-mono font-bold text-cyber-primary">{{ cert.cert_name }}</span>
                    <span class="block text-xs text-cyber-muted">({{ cert.cert_year }})</span>
                </div>
                {% endfor %}
            </div>
        </div>
    </div>
</section>
