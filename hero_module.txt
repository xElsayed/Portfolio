<section id="home" class="min-h-screen flex items-center pt-16 pb-20">
    <div class="w-full flex flex-col md:flex-row items-center justify-between gap-12">
        <!-- Text Content -->
        <div class="md:w-2/3">
            <span class="font-mono text-cyber-primary">Connecting to [{{ module.user_name }}]...</span>
            <h1 class="text-4xl md:text-6xl font-bold text-white my-4">{{ module.user_name }}</h1>

            <!-- Typing Animation -->
            <div class="font-mono text-2xl md:text-4xl text-cyber-secondary font-bold">
                <span class="text-cyber-primary">></span>
                <span id="typing-text" class="border-r-4 border-white"></span>
            </div>

            <div class="mt-6 text-lg md:text-xl max-w-3xl text-cyber-text">
                {{ module.bio }}
            </div>

            <div class="mt-8 flex flex-col sm:flex-row gap-4">
                <a href="#projects" class="inline-block px-8 py-3 bg-cyber-primary text-cyber-bg font-bold rounded-md hover:bg-green-500 transition-colors text-center">
                    [ View Projects ]
                </a>
                {% if module.cv_file %}
                <a href="{{ module.cv_file }}" download class="inline-block px-8 py-3 bg-cyber-surface text-cyber-primary font-mono rounded-md hover:bg-gray-700 transition-colors text-center">
                    Download_CV.pdf
                </a>
                {% endif %}
            </div>
        </div>

        <!-- Image Content -->
        <div class="md:w-1/3 w-full max-w-xs mx-auto mt-10 md:mt-0">
            {% if module.profile_photo.src %}
            <img src="{{ module.profile_photo.src }}" alt="{{ module.user_name }}" class="rounded-full border-4 border-cyber-primary shadow-lg shadow-cyber-primary/30 w-full h-auto object-cover">
            {% endif %}
        </div>
    </div>
</section>

<!-- Pass HubL Data to JS for Typing Animation -->
<script>
    window.typingRoles = [
        {% for item in module.typing_roles %}
            "{{ item }}",
        {% endfor %}
    ];
</script>
  
